using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StandbySlot : BattleSlot
{
    public StandbySlot(EntityType type, uint index) : base(type, index)
    {
    }
}
